import java.util.HashMap;

/**
 * Created by fshabbir on 7/8/17.
 */
public class Player {
    private HashMap<GemColor, Integer> currGems = new HashMap<GemColor, Integer>(GemColor.values().length);
    private Integer playerOrder = null;

    public void Player(Integer plyerOrder) {
        playerOrder = playerOrder;
    };
}
